'use strict';

require('chai').should();

describe('[batch]', function() {
    // const dummy = Object.assign({}, {}, data);
    /*
    it('collectLibBlock', async function() {
        return collectLibBlock()
        .then(function(result) {
            console.log(result);
        });
    });
    */
});
